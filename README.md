# ML--Payment-date-prediction
The ML payment date prediction notebook contains
an imported dataset payment-date invoice and the ML model
created using decision tree classifier predicts the payment dates.
